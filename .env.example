# === REQUIRED ===
# Your wallet private key (must have ETH for gas + USDC for payments on Base)
PINION_PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE

# Your wallet address (receives x402 payments from skill server callers)
AGENT_WALLET_ADDRESS=0xYOUR_WALLET_ADDRESS_HERE

# === OPTIONAL ===
# Network: "base" for mainnet, "base-sepolia" for testnet
PINION_NETWORK=base-sepolia

# Skill server port (default: 4020)
SKILL_SERVER_PORT=4020

# Price to charge per intelligence report
INTEL_PRICE=$0.08

# Profit threshold in USDC to trigger reinvestment
REINVEST_THRESHOLD=0.50

# Percentage of profit to reinvest (0.0 - 1.0)
REINVEST_PERCENTAGE=0.80
